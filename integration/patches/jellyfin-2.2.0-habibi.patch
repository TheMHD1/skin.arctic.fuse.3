diff --git a/jellyfin_kodi/downloader.py b/jellyfin_kodi/downloader.py
index c8eebd5..6eea21a 100644
--- a/jellyfin_kodi/downloader.py
+++ b/jellyfin_kodi/downloader.py
@@ -295,11 +295,14 @@ class GetItemWorker(threading.Thread):
 
     is_done = False
 
-    def __init__(self, server, queue, output):
+    def __init__(self, server, queue, output, item_filter=None, accepted=None):
 
         self.server = server
         self.queue = queue
         self.output = output
+        self.item_filter = item_filter
+        self.accepted = accepted
+        self.failed = False
         threading.Thread.__init__(self)
 
     def run(self):
@@ -324,13 +327,31 @@ class GetItemWorker(threading.Thread):
                 }
 
                 try:
+                    # IPTV catalogue scans broadcast to every permitted client.
+                    # Probe paths before requesting expensive complete metadata;
+                    # excluded items must never open a Kodi DB writer or progress UI.
+                    if self.item_filter:
+                        request["params"]["Fields"] = "Path"
+                        probe = self.server.http.request(request, s)
+                        eligible = [item["Id"] for item in probe["Items"]
+                                    if not self.item_filter(item)]
+                        LOG.info("Sync path filter: received=%s eligible=%s", len(item_ids), len(eligible))
+                        if not eligible:
+                            if window("jellyfin_should_stop.bool"):
+                                break
+                            continue
+                        request["params"]["Ids"] = ",".join(eligible)
+                        request["params"]["Fields"] = api.info()
                     result = self.server.http.request(request, s)
 
                     for item in result["Items"]:
 
-                        if item["Type"] in self.output:
+                        if item["Type"] in self.output and not (self.item_filter and self.item_filter(item)):
+                            if self.accepted:
+                                self.accepted()
                             self.output[item["Type"]].put(item)
                 except HTTPException as error:
+                    self.failed = True
                     LOG.error("--[ http status: %s ]", error.status)
 
                     if error.status == "ServerUnreachable":
@@ -339,9 +360,11 @@ class GetItemWorker(threading.Thread):
                         break
 
                 except Exception as error:
+                    self.failed = True
                     LOG.exception(error)
-
-                self.queue.task_done()
+                finally:
+                    self.queue.task_done()
 
                 if window("jellyfin_should_stop.bool"):
                     break
+        self.is_done = True
diff --git a/jellyfin_kodi/helper/utils.py b/jellyfin_kodi/helper/utils.py
index ed897e1..314cc31 100644
--- a/jellyfin_kodi/helper/utils.py
+++ b/jellyfin_kodi/helper/utils.py
@@ -558,6 +558,11 @@ def find_library(server, item):
 
     sync = get_sync()
     whitelist = [x.replace("Mixed:", "") for x in sync["Whitelist"]]
+    # Explicit unsynced IPTV libraries must not trigger per-item ancestry calls.
+    path = str(item.get("Path") or "").replace("\\", "/")
+    for prefix, library_id in sync.get("ExcludedLibraryPaths", {}).items():
+        if library_id not in whitelist and (path == prefix.rstrip("/") or path.startswith(prefix.rstrip("/") + "/")):
+            return {}
     ancestors = server.jellyfin.get_ancestors(item["Id"])
     for ancestor in ancestors:
         if ancestor["Id"] in whitelist:
diff --git a/jellyfin_kodi/library.py b/jellyfin_kodi/library.py
index fe4d577..b515255 100644
--- a/jellyfin_kodi/library.py
+++ b/jellyfin_kodi/library.py
@@ -32,6 +32,15 @@ LIMIT = int(settings("limitIndex") or 15)
 DTHREADS = int(settings("limitThreads") or 3)
 TARGET_DB_VERSION = 1
 
+
+def excluded_stream_item(item, sync):
+    """Skip only explicitly excluded, unsynced paths, including Season updates."""
+    whitelist = {value.replace("Mixed:", "") for value in sync.get("Whitelist", [])}
+    path = str(item.get("Path") or "").replace("\\", "/")
+    return any(library_id not in whitelist and
+               (path == prefix.rstrip("/") or path.startswith(prefix.rstrip("/") + "/"))
+               for prefix, library_id in sync.get("ExcludedLibraryPaths", {}).items())
+
 ##################################################################################################
 
 
@@ -59,6 +68,8 @@ class Library(threading.Thread):
         self.userdata_output = self.__new_queues__()
         self.removed_output = self.__new_queues__()
         self.notify_output = queue.Queue()
+        self.downloads_pending = False
+        self.downloads_failed = False
 
         self.jellyfin_threads = []
         self.download_threads = []
@@ -140,6 +151,7 @@ class Library(threading.Thread):
         Start new "daemon threads" to process library updates.
         (actual daemon thread is not supported in Kodi)
         """
+        self.downloads_failed |= any(thread.is_done and thread.failed for thread in self.download_threads)
         self.download_threads = [
             thread for thread in self.download_threads if not thread.is_done
         ]
@@ -224,9 +236,15 @@ class Library(threading.Thread):
             and not self.writer_threads["updated"]
             and not self.writer_threads["userdata"]
             and not self.writer_threads["removed"]
+            and not self.updated_queue.unfinished_tasks
+            and not self.userdata_queue.unfinished_tasks
+            and not self.removed_queue.unfinished_tasks
+            and not self.worker_queue_size()
         ):
             self.pending_refresh = False
-            self.save_last_sync()
+            self.downloads_pending = False
+            if not self.downloads_failed:self.save_last_sync()
+            self.downloads_failed = False
             self.total_updates = 0
             window("jellyfin_sync", clear=True)
 
@@ -251,6 +269,21 @@ class Library(threading.Thread):
                 if xbmc.getCondVisibility("Window.IsMedia"):
                     xbmc.executebuiltin("Container.Refresh")
 
+        # An excluded-only batch has no DB writer and no GUI refresh, but it
+        # still completed successfully. Advance the catch-up watermark so the
+        # next Kodi startup doesn't download the same excluded history again.
+        if (self.downloads_pending and not self.pending_refresh
+                and not self.download_threads
+                and not self.updated_queue.unfinished_tasks
+                and not self.userdata_queue.unfinished_tasks
+                and not self.removed_queue.unfinished_tasks
+                and not self.worker_queue_size()
+                and not any(self.writer_threads.values())):
+            if not self.downloads_failed:self.save_last_sync()
+            self.downloads_failed = False
+            self.downloads_pending = False
+            self.total_updates = 0
+
     def stop_client(self):
         self.stop_thread = True
 
@@ -282,7 +315,10 @@ class Library(threading.Thread):
         ):
             if work_queue[0].qsize() and len(self.download_threads) < DTHREADS:
 
-                new_thread = GetItemWorker(self.server, work_queue[0], work_queue[1])
+                policy = get_sync()
+                item_filter = (lambda item, policy=policy: excluded_stream_item(item, policy)) if policy.get("ExcludedLibraryPaths") else None
+                new_thread = GetItemWorker(self.server, work_queue[0], work_queue[1], item_filter, self.accepted_update)
+                self.downloads_pending = True
                 new_thread.start()
                 LOG.info("-->[ q:download/%s ]", id(new_thread))
                 self.download_threads.append(new_thread)
@@ -655,6 +691,10 @@ class Library(threading.Thread):
 
         return True
 
+    def accepted_update(self):
+        # Count only actual local work, not the entire excluded IPTV catalogue.
+        self.total_updates += 1
+
     def userdata(self, data):
         """Add item_id to userdata queue."""
         if not data:
@@ -665,7 +705,6 @@ class Library(threading.Thread):
         for item in split_list(items, LIMIT):
             self.userdata_queue.put(item)
 
-        self.total_updates += len(items)
         LOG.info("---[ userdata:%s ]", len(items))
 
     def updated(self, data):
@@ -676,7 +715,6 @@ class Library(threading.Thread):
         for item in split_list(data, LIMIT):
             self.updated_queue.put(item)
 
-        self.total_updates += len(data)
         LOG.info("---[ updated:%s ]", len(data))
 
     def removed(self, data):
@@ -714,6 +752,7 @@ class UpdateWorker(threading.Thread):
 
     def run(self):
         with self.lock, self.database as kodidb, Database("jellyfin") as jellyfindb:
+            sync_policy = get_sync()
             default_args = (self.server, jellyfindb, kodidb, self.direct_path)
             if kodidb.db_file == "video":
                 movies = Movies(*default_args)
@@ -736,6 +775,9 @@ class UpdateWorker(threading.Thread):
                     break
 
                 try:
+                    if excluded_stream_item(item, sync_policy):
+                        self.queue.task_done()
+                        continue
                     LOG.debug("{} - {}".format(item["Type"], item["Name"]))
                     if item["Type"] == "Movie":
                         movies.movie(item)
diff --git a/jellyfin_kodi/player.py b/jellyfin_kodi/player.py
index c3c1e6d..c549ffe 100644
--- a/jellyfin_kodi/player.py
+++ b/jellyfin_kodi/player.py
@@ -127,6 +127,13 @@ class Player(xbmc.Player):
             LOG.warning("Failed to report session playing: %s", e)
         window("jellyfin.skip.%s.bool" % item["Id"], True)
 
+        # Prepare before a seek can jump past the normal progress trigger.
+        if item.get("Type") == "Episode" and not self.up_next:
+            try:
+                self.up_next = bool(self.next_up())
+            except Exception as error:
+                LOG.warning("Could not prepare next episode yet: %s", type(error).__name__)
+
         # Immediate skip check for segments starting at 0:00
         if settings("mediaSegmentsEnabled.bool"):
             try:
@@ -278,7 +285,7 @@ class Player(xbmc.Player):
 
     def next_up(self):
         if not settings("enableUpNext.bool"):
-            return
+            return True
 
         item = self.get_file_info(self.get_playing_file())
         objects = Objects()
@@ -298,9 +305,12 @@ class Player(xbmc.Player):
                 except IndexError:
                     LOG.warning("No next up episode.")
 
-                    return
+                    return True
 
                 break
+        else:
+            LOG.warning("Current episode absent from adjacent episode response")
+            return False
         server_address = item["Server"].auth.get_server_info(
             item["Server"].auth.server_id
         )["address"]
@@ -327,6 +337,7 @@ class Player(xbmc.Player):
 
         LOG.info("--[ next up ] %s", next_info)
         event("upnext_data", next_info, hexlify=True)
+        return True
 
     def onPlayBackPaused(self):
         current_file = self.get_playing_file()
@@ -401,8 +412,7 @@ class Player(xbmc.Player):
 
             if played > 2.0 and not self.up_next:
 
-                self.up_next = True
-                self.next_up()
+                self.up_next = bool(self.next_up()) if item.get("Type") == "Episode" else True
 
             if (item["CurrentPosition"] - previous) < 30:
                 return
@@ -614,8 +624,7 @@ class Player(xbmc.Player):
             )
 
             if segment_type == "Credits" and not self.up_next:
-                self.up_next = True
-                self.next_up()
+                self.up_next = bool(self.next_up())
 
             self._handle_skip_segment(segment_type, start, end, skip_mode)
             break
