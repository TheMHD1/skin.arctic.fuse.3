[Service]
# The independent oneshot verifies its own profile and identity.  Ignore its
# result here so an NFS bridge fault cannot mark Docker failed.
ExecStartPost=-/usr/bin/systemctl start --no-block am9-nfs-forward.service
